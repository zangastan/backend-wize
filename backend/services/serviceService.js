const services = require("../models/servicesModel")

const getAllSerevices = async () => {
    try {
        
    }catch(error){
        return {error : error.message}
    }
}

module.exports = {
    getAllSerevices,
    
}